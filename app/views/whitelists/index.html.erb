<h1>Whitelist Management</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Expiration Date</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @whitelists.each do |whitelist| %>
      <tr>
        <td><%= whitelist.email %></td>
        <td><%= whitelist.expires_at || "Never" %></td>
        <td>
          <%= link_to "Show", whitelist_path(whitelist) %> |
          <%= link_to "Edit", edit_whitelist_path(whitelist) %> |
          <%= link_to "Remove", delete_whitelist_path(whitelist) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Add Whitelist Entry", new_whitelist_path %>
<%= link_to "Back", root_path %>
