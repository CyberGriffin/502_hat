<h1>Remove Whitelist Entry</h1>

<div>
    <%= form_with model: @whitelist, url: whitelist_path(@whitelist), method: :delete, local: true do |f| %>
    
    <p>Are you sure you want to permanently remove this email from the whitelist?</p>

    <p class="reference-name"><strong><%= @whitelist.email %></strong></p>

    <div class="form-buttons">
      <%= f.submit("Yes, Remove", class: "btn btn-danger") %>
    </div>

  <% end %>

  <%= link_to "Cancel", whitelists_path, class: "btn btn-secondary" %>
</div>